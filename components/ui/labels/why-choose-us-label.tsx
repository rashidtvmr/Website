import styled from "styled-components";

export const WhyChooseUsLabel = ({boldText, notBoldText}) => {
    return (
        <div>not bold part <span style={{fontWeight: "bold"}}>Bold part</span></div>
    )
}